{
  "name": "hostera24-influencers",
  "private": true,
  "scripts": {
    "db:reset": "bash scripts/db-reset.sh --yes",
    "db:reset:demo": "bash scripts/db-reset.sh --yes --demo",
    "db:migrate": "bash scripts/vps-migrate.sh",
    "dev:all": "bash scripts/dev-all.sh"
  }
}
